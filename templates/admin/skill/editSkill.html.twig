{% extends 'base.html.twig' %}

{% block title %}Admin{{ parent() }}{% endblock %}

{% block body %}
    <h1 class="adminTitle">Modifier la compétence "{{ skill.name }}"</h1>
    {{ include('admin/skill/_formSkill.html.twig', {'button_label': 'Modifier'}) }}
    <a href="{{ path('admin_skill_show', {'id' : skill.id }) }}">Retour à la compétence</a>
{% endblock %}
